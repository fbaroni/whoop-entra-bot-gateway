# API configuration
API_BASE_URL=http://localhost:3000
API_KEY=                          # Optional - leave empty for no auth (localhost only)
LOG_LEVEL=info

# WHOOP integration (get credentials from developer.whoop.com)
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:3000/api/whoop/callback
